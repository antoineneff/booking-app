# Booking app

## Install

### Backend
```bash
$ cd backend
$ npm install
# Seed database (create rooms)
# Check that mongo is running before executing next command
$ npm run seed
$ npm start
```

### Frontend
```shell
$ cd frontend
$ npm install
$ npm run serve
```
